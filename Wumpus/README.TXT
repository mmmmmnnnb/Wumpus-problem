Team member: Tianyu Jin, Xianhao Wang.

To run the program, need to use command prompt and run the PA4.py code.
It was really wierd that I can only make my code work in one of CMD prompt or VS code. Because the CWD on VS code and command prompt are different.
But it seems we will run the code on CMD.